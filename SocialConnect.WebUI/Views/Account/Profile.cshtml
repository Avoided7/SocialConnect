@model User

@{
    ViewData["Title"] = Model.UserName;
}

<div class="card">
    <div class="card-header">
        <h3>Profile</h3>
    </div>
    <div class="card-body">
        <p>@Model.UserName</p>
        <b>@Model.DateOfBirth.ToShortDateString()</b>
    </div>
</div>